# SMTP Configuration
SMTP_PROVIDER=gmail  # Options: gmail, outlook, yahoo
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password  # For Gmail, use App Password

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=forex_news
DB_USER=forex_user
DB_PASSWORD=DBPASSWORD

# Redis Configuration
USE_REDIS=false  # Set to true if you want to use Redis for rate limiting
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=  # Leave empty if no password is set

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
DEBUG=False  # Set to False in production
SECRET_KEY=your-secret-key-here  # Generate a strong secret key
FLASK_ENV=production  # Set to 'development' for development

# SMTP Configuration
SMTP_PROVIDER=gmail  # Options: gmail, outlook, yahoo
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password  # For Gmail, use App Password

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=forex_news
DB_USER=forex_user
DB_PASSWORD=DBPASSWORD

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
DEBUG=True 