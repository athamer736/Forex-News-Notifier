# SMTP Configuration
SMTP_PROVIDER=gmail  # Options: gmail, outlook, yahoo
SMTP_HOST=smtp.gmail.com  # Optional: Will use default based on provider if not set
SMTP_PORT=587  # Optional: Will use default based on provider if not set
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password  # For Gmail, use App Password 